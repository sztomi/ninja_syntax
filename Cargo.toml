[package]
name = "ninja_syntax"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
textwrap = "0.16.0"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "2.1"
tempdir = "0.3"
